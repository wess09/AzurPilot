"""上游历史重写基线。

上游清理历史（例如移除被替换掉的模型、构建产物）后，已经 clone 过的本地仓库会停在
重写前的旧历史上。此时更新入口用 `git rev-list --left-right --count <local>...<branch>`
统计时会把整条旧链算成 ahead，于是判定"本地有独有提交"而拒绝更新——但那些提交只是
旧版上游历史，并非用户自己写的。

这里记录每次历史重写前的分支顶端，用于识别这种"假 ahead"：本地 HEAD 只要是其中某个
基线的祖先，就说明它停留在旧版上游历史上，可以照常更新（更新走 reset --hard，会把它
对齐到重写后的新历史）。

维护：每次上游重写历史后，把下面的 SHA 换成**重写当日**各分支的顶端提交，否则新
clone 的用户会被误判。全部客户端迁移完成后可以清空本列表。
"""

# 历史重写前的分支顶端提交。
REWRITE_BASES = (
    # dev
    '2223a4a13c25099add3b1f9d1b5029b6f2587b99',
    # master
    '08b4ef50b6b481a6e8ec4c47c174be3068493b45',
)
